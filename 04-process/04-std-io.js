#!/usr/bin/node


const msg=['name:','email','QQ','mobile'];

var i=0;
var me={};
console.log(msg[i++]+':');

process.stdin.on('data',(data)=>{

  
 var cmd="me."+msg[i-1]+ console.log(data.slice(0,data.length-1).toString('utf8'));
  console.log()
});
